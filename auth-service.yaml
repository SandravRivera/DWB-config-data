server:
  port: 8082

spring:
  application:
    name: auth
  
  datasource:
    #Cambien aqui por su BD
    url: jdbc:mysql://localhost:3306/dwb2026_2_auth
    username: root
    password: sql#04
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
